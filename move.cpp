#include "move.hpp"
using namespace std;

Move::Move(int x0, int y0) {
    //конструктор, в котором хранятся две переменные, чтобы в дальнейшем использовать их как координаты.
    x = x0;
    y = y0;
}
